# Let Nixpacks auto-detect Python, add Node.js for frontend build
[phases.setup]
nixPkgs = ["nodejs_20"]

# After Python deps are installed, build frontend
[phases.build]
cmds = ["cd ksl-web && npm install && npm run build"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
